---
name: CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    paths-ignore:
      - ""
jobs:
  kafkacluster-api:
    runs-on:
      - self-hosted
      - scrbsmqdk007491
    name: validation
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: validation
        id: files
        run: |
          host: scrbsmqdk007497
          username: pkh045
          password: yc]%2z\93C(cB[WE
          port: 22
          script: |
            mkdir -p /home/pkh045/test
